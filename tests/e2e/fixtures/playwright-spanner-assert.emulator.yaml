schemaFile: ./schema/schema.sql
defaultExpectedData: ./expected/default.yaml
database:
  projectId: e2e-project
  instanceId: e2e-instance
  database: e2e-database
spalidate:
  command: spalidate
  args:
    - --project
    - "{projectId}"
    - --instance
    - "{instanceId}"
    - --database
    - "{databaseName}"
    - "{expectedFile}"
